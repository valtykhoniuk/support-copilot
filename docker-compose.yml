services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_db
      - logs_data:/app/logs
    restart: unless-stopped

volumes:
  chroma_data:
  logs_data:
